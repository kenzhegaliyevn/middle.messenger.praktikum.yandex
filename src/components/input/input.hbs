<input type="{{ type }}" class="{{ class }}" name="{{ name }}"
{{#if value}}
 value="{{ value }}"
{{/if}}
{{#if validationType }}
 data-validation="{{ validationType }}"
{{/if}}
{{#if placeholder}}
 placeholder="{{ placeholder }}"
{{/if}}
{{#if accept}}
 accept="{{ accept }}"
{{/if}}
>
